{
    "core-crawling": {
      "image": ":core-crawling-service.core-crawling.latest",
      "ports": {
        "8001": "HTTP"
      },
      "environment": {
        "PUBLIC_IP": "${PUBLIC_IP}",
        "BACKEND_PORT": "${BACKEND_PORT}",
        "CORE_PORT": "${CORE_PORT}"
      }
    }
}